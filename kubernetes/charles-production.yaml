apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-charles-services
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prod-charles-services
  template:
    metadata:
      labels:
        app: prod-charles-services
    spec:
      imagePullSecrets:
        - name: acr-pull-secret
      containers:
        - name: prod-charles-services
          image: placeholder # replaced by pipeline via kubectl set image
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: prod-charles-services-service
  namespace: default
spec:
  selector:
    app: prod-charles-services
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
